name: Demo Failed Workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        cd 04-ai-github-actions-healer
        python -m pip install --upgrade pip
        pip install requests
        # Intentionally NOT installing pytest - will cause failure!
    
    - name: Run tests (THIS WILL FAIL)
      run: |
        cd 04-ai-github-actions-healer
        # This fails because pytest is not installed
        python -m pytest tests/