name: Demo Failed Workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        cd 04-ai-github-actions-healer
        python -m pip install --upgrade pip
        pip install requests
        pip install pytest
       
    
    - name: Run tests (THIS WILL PASS)
      run: |
        cd 04-ai-github-actions-healer
        python -m pytest tests/